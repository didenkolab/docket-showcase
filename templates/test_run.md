---
type: test_run
result: todo
ran_at: 
evidence: 
---

## What happened

For a failure: what was observed, not what was expected — the expectation is in
the test. Link the defect with `found:` so the bug shows the run that found it.

Point at the test with `runs:` and put this under its execution with `parent:`.
